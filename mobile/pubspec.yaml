name: mobile
description: A new Flutter project.

environment:
  sdk: '^3.8.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  web_socket_channel: ^2.4.0

flutter:
  uses-material-design: true
